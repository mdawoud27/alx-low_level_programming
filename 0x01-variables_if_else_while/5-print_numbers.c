#include <stdio.h>

/**
 * main - entry point
 * program prints numbers base 10
 * Return: 0 always wins
 */

int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
