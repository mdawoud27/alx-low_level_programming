#include <string.h>
#include "main.h"

/**
 * _strcpy - function that copies the string
 * @src: string should be copied
 * @dest: string copies from src
 * Return: the pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
